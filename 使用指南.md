# 🎓 AI 教学助手系统 - 使用指南

## 📦 项目已创建完成

恭喜！您的 AI 教学助手系统已经完整创建，包含以下内容：

### 核心功能
1. **AI 作业批改助手** - 拍照上传，自动批改，统计分析
2. **AI 错题本管家** - 智能录入，变式题生成，掌握追踪
3. **AI 教案生成** - 一键生成完整教案，支持导出

### 文件统计
- **后端文件**: 10 个（FastAPI + SQLAlchemy）
- **前端文件**: 13 个（React + Tailwind CSS）
- **配置文件**: 7 个（Docker + 环境配置）
- **文档文件**: 5 个（完整使用说明）
- **总计**: 35+ 个文件，3000+ 行代码

---

## 🚀 快速启动（3 步即可）

### 步骤 1：配置 API 密钥

编辑文件 `backend/.env`（从 `.env.example` 复制）：

```bash
# 将你的 AI API 密钥填入这里
AI_API_KEY=sk-your-actual-api-key-here

# 其他配置保持默认即可
AI_API_BASE_URL=https://api.openai.com/v1
AI_MODEL=gpt-4
OCR_MODEL=gpt-4-vision-preview
```

**重要**: 必须配置 `AI_API_KEY` 才能使用 AI 功能！

### 步骤 2：启动服务

**方式 A：使用 Docker（推荐）**

```bash
# 在项目根目录执行
docker-compose up -d

# 查看运行状态
docker-compose ps

# 查看日志
docker-compose logs -f
```

**方式 B：本地开发模式**

**Windows 用户：**
```bash
# 双击运行
start.bat

# 或手动执行
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload
```

**在新终端窗口：**
```bash
cd frontend
npm install
npm run dev
```

**Linux/Mac 用户：**
```bash
chmod +x start.sh
./start.sh
```

### 步骤 3：访问应用

打开浏览器访问：
- **前端界面**: http://localhost:3000
- **后端 API**: http://localhost:8000
- **API 文档**: http://localhost:8000/docs

---

## 📖 功能使用说明

### 1️⃣ AI 作业批改

**使用场景**: 批改学生作业、试卷

**操作步骤**:
1. 点击"上传作业照片"区域
2. 拖拽或选择图片（支持多张）
3. （可选）填写参考答案
4. 点击"开始批改"
5. 等待 AI 识别和批改
6. 查看详细结果和解析

**支持格式**: JPG, PNG, GIF
**文件大小**: 最大 10MB

**批改结果包含**:
- ✅ 总题数、正确数、错误数
- ✅ 每道题的详细批改
- ✅ 学生答案 vs 正确答案
- ✅ 题目解析

---

### 2️⃣ AI 错题本

**使用场景**: 整理错题、举一反三

**操作步骤**:
1. 切换到"错题本"页面
2. 点击"上传"按钮
3. 选择错题图片
4. 选择知识点（如：三角函数、导数）
5. 系统自动识别并生成变式题
6. 查看错题详情和变式练习

**功能亮点**:
- 📌 自动 OCR 识别题目
- 🎯 按知识点分类
- 💡 生成 3 道变式题
- ✅ 标记"已掌握"状态
- 🔍 筛选查找

---

### 3️⃣ AI 教案生成

**使用场景**: 备课、教学设计

**操作步骤**:
1. 切换到"教案生成"页面
2. 填写课题名称（必填）
3. 选择课时数（1-3 课时）
4. 选择学生基础（基础薄弱/中等/培优）
5. 填写额外要求（可选）
6. 点击"生成教案"
7. 预览并导出

**教案内容包含**:
- 📋 教学目标
- 🎯 教学重难点
- 📚 教学过程设计
- 📝 板书设计
- 💭 教学反思

**导出格式**: Word (.docx), PDF (.pdf)

---

## 🔧 常见问题

### Q1: 提示"OCR 识别失败"
**原因**: API 密钥未配置或网络问题
**解决**: 
1. 检查 `backend/.env` 中的 `AI_API_KEY`
2. 确保网络连接正常
3. 查看后端日志：`docker-compose logs backend`

### Q2: 前端无法连接后端
**原因**: 后端服务未启动
**解决**:
1. 检查后端是否运行：访问 http://localhost:8000/health
2. 重启服务：`docker-compose restart`

### Q3: 图片上传失败
**原因**: 上传目录权限问题
**解决**:
```bash
# Docker 模式下
docker-compose down
docker-compose up -d
```

### Q4: 如何更换 AI 模型？
**解决**: 编辑 `backend/.env`:
```env
# 使用其他兼容的 API
AI_API_BASE_URL=https://your-custom-api.com/v1
AI_MODEL=gpt-3.5-turbo
```

---

## 📊 系统架构

```
用户浏览器 (http://localhost:3000)
    ↓
React 前端 (Vite + Tailwind CSS)
    ↓ HTTP/REST API
FastAPI 后端 (http://localhost:8000)
    ↓
├─ AI 客户端 (OCR + 解题 + 生成)
├─ SQLite 数据库
└─ 文件存储 (uploads/)
    ↓
外部 AI 服务 (OpenAI API 等)
```

---

## 🗂️ 项目结构

```
AI 辅助教学工具/
├── backend/              # 后端服务
│   ├── main.py          # 应用入口
│   ├── api.py           # API 路由
│   ├── models.py        # 数据模型
│   └── ai_client.py     # AI 调用
│
├── frontend/            # 前端应用
│   └── src/
│       ├── pages/       # 页面组件
│       ├── components/  # 通用组件
│       └── utils/       # 工具函数
│
├── docker-compose.yml   # Docker 配置
├── README.md            # 完整文档
└── start.bat / start.sh # 启动脚本
```

---

## 🎯 下一步建议

### 立即体验
1. ✅ 配置 API 密钥
2. ✅ 运行 `docker-compose up -d`
3. ✅ 访问 http://localhost:3000
4. ✅ 测试三个核心功能

### 功能扩展
- 添加用户登录系统
- 实现班级作业布置
- 生成学情分析报告
- 支持更多科目（物理、化学）

### 生产部署
- 配置 Nginx 反向代理
- 申请 HTTPS 证书
- 使用 PostgreSQL 数据库
- 配置对象存储（OSS）

---

## 📞 技术支持

### 查看日志
```bash
# 后端日志
docker-compose logs backend

# 前端日志
docker-compose logs frontend

# 实时日志
docker-compose logs -f
```

### 重启服务
```bash
docker-compose restart

# 或完全重建
docker-compose down
docker-compose up -d --build
```

### 数据库位置
- SQLite 文件：`backend/data/teaching_assistant.db`
- 上传文件：`backend/uploads/`

---

## 🎉 开始使用

现在您已经准备好了！

**快速命令**:
```bash
# 启动
docker-compose up -d

# 停止
docker-compose down

# 查看状态
docker-compose ps

# 访问
# http://localhost:3000
```

**祝您使用愉快！** 🚀

---

*提示：建议先使用测试图片尝试基本功能，熟悉后再批量使用。*
